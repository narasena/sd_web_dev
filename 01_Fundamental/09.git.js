// git init => untuk inisialisasi git tracking di project kita

// git commit -m "message" => untuk menyimpan perubahan di local repository
// git commit -m "initial commit"

// git add <files> =>  untuk menambah file/code/line yang akan kita taruh di staging area
// git add . => menambah semua perubahan yang ada ke staging area

// git log => menampilkan commit-commit